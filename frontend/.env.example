# The frontend calls the API with relative paths (/api/...) by default.
# Set this only if the API lives on a different origin (e.g. a standalone backend host).
# VITE_API_URL=https://api.example.com
